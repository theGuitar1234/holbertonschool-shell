#!/usr/bin/env bash
#Prints Best School 10 times but also prints Hi at 9th iteration
remainder=0
while [ "$remainder" -ne 10 ]
do
        _=$((remainder += 1))
        echo "Best School"
        if [ "$remainder" -eq 9 ]; then
                echo "Hi"
        fi
done
